const { assert } = require('chai');
const User = require('../../lib/models/User');

describe('User model', () => {

    const user = {
        email: 'me@me.com'
    };

    const password = 'abc';

    it('generates hash from password', () => {
        
    });
});